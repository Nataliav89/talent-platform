import React from 'react'
import ReactDOM from 'react-dom/client'
import TalentPlatform from './TalentPlatform'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TalentPlatform />
  </React.StrictMode>,
)
